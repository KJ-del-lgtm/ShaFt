<h2>グループ詳細</h2>

<p><strong>ID:</strong> <%= @group.id %></p>
<p><strong>グループ名:</strong> <%= @group.name %></p>
<p><strong>作成者:</strong> <%= @group.owner.name %></p>
<p><strong>作成日:</strong> <%= l(@group.created_at, format: :long) %></p>

<h4>参加ユーザー</h4>
<ul>
  <% @group.users.each do |user| %>
    <li><%= user.name %></li>
  <% end %>
</ul>

<%= link_to "削除", admin_group_path(@group),method: :delete, data: { confirm: "本当に削除しますか？" }, class: "btn btn-danger mt-3" %>
<%= link_to "戻る", admin_groups_path, class: "btn btn-secondary mt-3" %>